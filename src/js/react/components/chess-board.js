import React from 'react';

const ChessBoard = React.createClass({
    render () {
        return (
            <div>
                board
            </div>
        );
    }
});

export default ChessBoard;